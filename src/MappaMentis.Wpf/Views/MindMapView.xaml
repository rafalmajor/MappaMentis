<UserControl x:Class="MappaMentis.Wpf.Views.MindMapView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:MappaMentis.Wpf.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="1000">
    <Grid Background="{StaticResource BackgroundBrush}">
        <StackPanel VerticalAlignment="Top" HorizontalAlignment="Left" Margin="20">
            <TextBlock Text="{Binding MindMapTitle}" 
                       FontSize="28" 
                       FontWeight="Bold"
                       Foreground="{StaticResource PrimaryBrush}"
                       Margin="0,0,0,10"/>
            <TextBlock Text="{Binding StatusMessage}" 
                       FontSize="14"
                       Foreground="{StaticResource PrimaryLightBrush}"
                       Margin="0,0,0,20"/>
            <Button Content="Back to Dashboard" 
                    Padding="16,8"
                    FontSize="12"
                    Command="{Binding BackToDashboardCommand}"
                    />
        </StackPanel>
        <Canvas Background="White" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Margin="0,120,0,0"/>
    </Grid>
</UserControl>
