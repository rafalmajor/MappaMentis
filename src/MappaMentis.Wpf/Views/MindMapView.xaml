<UserControl x:Class="MappaMentis.Wpf.Views.MindMapView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:MappaMentis.Wpf.ViewModels"
             xmlns:local="clr-namespace:MappaMentis.Wpf.Views"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="1200">
    <Grid Background="{StaticResource BackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header with title and back button -->
        <!-- <StackPanel Grid.Row="0" VerticalAlignment="Top" HorizontalAlignment="Left" Margin="20"> -->
        <!--     <TextBlock Text="{Binding MindMapTitle}"  -->
        <!--                FontSize="28"  -->
        <!--                FontWeight="Bold" -->
        <!--                Foreground="{StaticResource PrimaryBrush}" -->
        <!--                Margin="0,0,0,10"/> -->
        <!--     <Button Content="Back to Dashboard"  -->
        <!--             Padding="16,8" -->
        <!--             FontSize="12" -->
        <!--             Command="{Binding BackToDashboardCommand}" -->
        <!--             /> -->
        <!-- </StackPanel> -->

        <!-- Mind map radial visualization -->
        <local:MindMapRadialView x:Name="RadialView"
                                Grid.Row="1"
                                Background="White"/>
    </Grid>
</UserControl>
